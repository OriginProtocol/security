# Reproduction of Cover Protocol exploit

The exploit is implemented as a Hardhat task in `hardhat.config.js`.

To run use:

`PROVIDER_URL=https://eth-mainnet.alchemyapi.io/v2/<key> yarn run exploit`
